# BAIGE for Unreal Engine

首要支持引擎。计划提供 Editor Bridge、代理导出、权威预览、数据编译器、Runtime Executor 与 PIE 调试回传。

此目录中的通用数据不得依赖 Unreal 私有序列化；引擎专属实现放入独立插件模块。

BAIGE 是共享技术层。未来具体领域工具通过自己的独立产品名接入本目录提供的 Unreal Bridge，并统一使用 `A BAIGE Project` 署名。
