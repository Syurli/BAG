# Security Policy

## 报告安全问题

请不要公开披露涉及任意文件读写、命令执行、路径穿越、密钥泄露、远程模型数据外传或 Bridge 未授权访问的问题。请通过 GitHub 仓库的 Private vulnerability reporting 提交。

## 默认安全模型

- Bridge 默认仅监听本机回环地址。
- 网络能力、遥测和远程 LLM 默认关闭。
- LLM 仅接收用户明确选择的结构化数据。
- 编辑器不执行来自项目数据的任意脚本。
- 所有写入操作必须经过路径白名单和 Schema 校验。

当前项目处于早期开发阶段，尚未承诺生产安全等级。
