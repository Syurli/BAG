# AGENTS.md

本文件是 Codex 及其他代码代理在 CrossLoom 仓库中的最高优先级工程约束。任何子目录可增加更严格的 `AGENTS.md`，但不得削弱本文件要求。

## 1. 项目目标

CrossLoom 是本地优先、跨引擎、人工主导 AI 的游戏内容编辑平台。代理必须优先保护：数据可迁移性、人工审核权、引擎中立性、可回滚性与长期兼容性。

## 2. 不可破坏的边界

1. 不得让 LLM 直接修改或覆盖最终引擎资产。
2. 不得在通用 IR 中引入 Unreal、Unity、Godot 专属路径或类型名。
3. 不得未经 RFC 与迁移器修改稳定 Schema 的字段语义。
4. 不得以 `any`、关闭严格模式或跳过校验规避类型设计。
5. 不得提交密钥、Token、用户资产、生成缓存或正式项目内容。
6. 不得默认将遥测、网络上传或远程模型调用设为开启。
7. 不得手工修改标记为 generated 的文件；应修改源 Schema 或生成器。
8. 不得把网页预览结果声明为引擎权威结果。

## 3. 每个任务的必备输入

任务说明必须明确：

- 用户结果；
- 允许修改的目录；
- 禁止修改的接口；
- 输入与输出格式；
- 验收条件；
- 必须运行的测试；
- 文档更新范围；
- 回退方式。

缺少关键条件时，优先从现有规范、测试与代码推导；无法安全推导时停止破坏性修改。

## 4. 实现规则

- TypeScript 必须启用严格模式。
- 公共 API 先写类型、契约测试和错误语义，再写实现。
- 领域逻辑不得依赖 UI 框架或具体引擎 SDK。
- Bridge 只负责能力发现、资源映射、预览、编译和运行状态回传。
- 所有持久化变更必须可撤销、可重放、可生成稳定 Diff。
- 坐标、单位、时间、插值、生命周期必须显式声明。
- 新功能必须说明不支持时的降级行为。

## 5. 验证要求

提交前至少执行：

```bash
pnpm format:check
pnpm typecheck
pnpm test
pnpm build
```

若某项尚未配置，应在结果中明确说明，而不是伪造通过状态。

涉及 Schema、坐标转换、时间语义或 Bridge 的改动还必须增加 Golden Test。

## 6. 变更输出

代理完成任务后必须报告：

- 修改了什么；
- 为什么这样修改；
- 哪些测试已运行；
- 仍存在什么风险；
- 是否包含兼容性变化；
- 是否需要人工体验验收。

## 7. 人工保留决策

以下事项必须由维护者最终确认：产品范围、IR 语义、破坏性迁移、引擎能力降级、LLM 权限、安全策略、战斗编辑体验和正式发布。
